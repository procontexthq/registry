[project]
name = "procontext-registry"
version = "0.1.0"
description = "Registry tooling for ProContext — validation and checksum management"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = [
    "httpx>=0.27",
]

[dependency-groups]
dev = []
